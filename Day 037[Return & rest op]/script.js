//Print and Return

//Return is used to send the result outside the function
function sum(a,b) {
    var s= a+b;
    return s;
    //return must be the last statement in the function
    // return always takes the single value

}

// the function call is replaced by the return value
let result = sum(44,55)
console.log(result);

